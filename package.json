{
  "name": "google-calendar-mcp",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "prestart": "npm run build",
    "build": "tsc -p .",
    "start": "node --enable-source-maps dist/http.js",
    "dev": "tsx watch src/http.ts"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.7.0"
  }
}
