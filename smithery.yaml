runtime: "typescript"

build:
  install: "npm install"     # <--- era "npm ci"
  command: "npm run build"

startCommand:
  type: "http"
  command: "node"
  args: ["dist/index.js"]

env:
  PORT: "0"
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""
  GOOGLE_SCOPES: "https://www.googleapis.com/auth/calendar"  # lo ridurremo dopo a readonly
  CALENDAR_ID: ""
