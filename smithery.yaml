runtime: "typescript"

build:
  install: "npm ci"
  command: "npm run build"

startCommand:
  type: "http"
  command: "node"
  args: ["dist/http.js"]   # ✅ non "dist/index.js"

# ❌ NIENTE PORT qui: la inietta Smithery automaticamente
env:
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""
  GOOGLE_SCOPES: "https://www.googleapis.com/auth/calendar"
  CALENDAR_ID: ""
