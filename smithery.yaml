# smithery.yaml (corretto)
runtime: "typescript"

build:
  install: "npm ci"
  command: "npm run build"

startCommand:
  type: "http"
  command: "node"
  args: ["dist/http.js"]   # <-- era dist/index.js

# IMPORTANTE: non forzare PORT=0
env:
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""
  GOOGLE_SCOPES: "https://www.googleapis.com/auth/calendar"
  CALENDAR_ID: ""
