runtime: "typescript"

build:
  install: "npm ci"
  command: "npm run build"   # ok: compila TS → dist, e Smithery esegue ANCHE il suo build wrapper

startCommand:
  type: "http"
  command: "node"
  args: [".smithery/index.cjs"]   # 👈 avvia il wrapper generato dal CLI

# NON impostare PORT: la inietta Smithery
env:
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""
  GOOGLE_SCOPES: "https://www.googleapis.com/auth/calendar"
  CALENDAR_ID: ""
